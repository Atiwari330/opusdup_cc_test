{
  "name": "ehr-live-transcription-extension",
  "version": "1.0.0",
  "description": "Chrome Extension for live audio transcription during video calls, integrated with EHR AI analysis",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "package": "zip -r ehr-transcription-extension.zip manifest.json background.js offscreen.js offscreen.html sidepanel.js sidepanel.html sidepanel.css icons/"
  },
  "dependencies": {
    "@types/chrome": "^0.0.270"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}